import { Request, Response } from 'express'

/**
 * POST /mailer
 * Send mail to <xyz@schoen-klinik.com>.
 */
const postMail = (req: Request, res: Response) => {
  // placeholder for mailer functionality
}

export default postMail
